[project]
name = "llm-ai"
version = "0.1.0"
description = "Personal local AI software engineer"
requires-python = ">=3.13"

dependencies = [
    "ollama",
    "typer",
    "rich",
    "chromadb",
    "sentence-transformers"
]